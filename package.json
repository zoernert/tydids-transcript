{
  "name": "transcript-summarizer-spa",
  "displayName": "Transcript Summarizer SPA",
  "description": "A SPA to transcribe and summarize audio using Google Gemini AI.",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "cd webview-ui && vite",
    "build": "cd webview-ui && vite build"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.0.2",
    "@tsconfig/svelte": "^5.0.2",
    "@types/node": "18.x",
    "svelte": "^4.2.9",
    "svelte-check": "^3.6.3",
    "svelte-preprocess": "^5.1.3",
    "typescript": "^5.3.3",
    "vite": "^5.0.12"
  },
  "dependencies": {
    "@google/generative-ai": "^0.7.1"
  }
}
